Future features to implement:

- RAG Node (retrieval + LLM generation)
- Memory Node (conversation history)
- Output Node (formatted display)
- Query workflow (Input → RAG → Memory → Output)
- Chat interface
